// hello.nil — NilLang Hello World
// Run: nilc -in examples/hello/main.nil -run

function greet(name: string): string {
    return "Hello, " + name + "! Welcome to NilX Framework."
}

function main(): void {
    print("=== NilLang Hello World ===")

    let msg: string = greet("NilOS")
    print(msg)

    let msg2: string = greet("World")
    print(msg2)

    print("NilX Framework is running!")
}

main()
